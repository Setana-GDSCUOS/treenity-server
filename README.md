# treenity-server

### gradle build (Local, Windows)

```shell
$ gradlew build
```

### docker build & push (Local, WSL)

```shell
$ bash docker-push.sh
```

### docker pull & run (Prod, Linux)

```shell
$ bash docker-pull.sh
```
